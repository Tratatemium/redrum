# Redrum

A WebVR project built with [A-Frame](https://aframe.io/) and [Vite](https://vitejs.dev/).

## Structure

The scene is composed of modular rooms included into a single A-Frame scene:

## Getting Started

Install dependencies:

```bash
pnpm install
```

Start the dev server:

```bash
pnpm dev
```

Build for production:

```bash
pnpm build
```

Preview the production build:

```bash
pnpm preview
```

## Tech Stack

- [A-Frame 1.7.1](https://aframe.io/) — WebVR framework
- [Vite](https://vitejs.dev/) — build tool
- [vite-plugin-html-include](https://github.com/Tratatemium/redrum) — HTML `<include>` support for modular room files
